{
  "name":"Food-delar",
  "version": "1.0.1",
  "engines": {
        "node": ">=18 <19"
    },
    "author": "Infant",
  "scripts": {
    "start": "cd backend/built && node server.js",
    "prebuild": "cd backend && npm run build",
    "build":"cd main && npm run build"
 
  },
  "dependencies": {
    "pako": "^2.1.0"
  }
}
